describe('transload', () => {
  test('can be done with an API of a readable stream from one URL to another', async () => {
    const source_url = new URL('http://iscaliforniaonfire.com/')
  
  })

})
